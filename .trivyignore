# Waived CVEs for the cd-registry Trivy scan.
# Add one CVE ID per line WITH a comment explaining why it is safe to ignore
# (e.g. not exploitable in our usage, or no upstream fix shipped to the base image yet).
# Keep this list short and review it periodically.
