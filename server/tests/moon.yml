# Backend integration-test harness — its jest suite runs against a composed
# stack via `just test integration`, not a moon `:test` task; `language: javascript` still gives it `deps` and registers it with moon.
language: 'javascript'
