services:
  sensor-service:
    cpuset: "0-10"
    build:
      context: .
    environment:
      - MONGO_URI=mongodb://sensor-db:27017/sensordb
      - CLIENT_URL=${DEV_URL}:${CLIENT_PORT}
      - REDIS_URL=redis://redis:6379
    depends_on:
      - sensor-db

  sensor-db:
    cpuset: "0-10"
    image: mongo:latest
    volumes:
      - sensor-data:/data/db

volumes:
  sensor-data:
