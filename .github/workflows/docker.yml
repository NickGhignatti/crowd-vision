name: Docker Build

on:
  pull_request:
    branches: [ "master" ]
    paths:
      - "docker-compose.yml"
      - "docker-compose.dbs.yml"
      - "docker-compose.services.yml"
      - "docker-compose.simulators.yml"
      - "docker-compose.client.yml"
      - "docker-compose.interfaces.yml"
      - "docker-compose.dev.yml"
      - "server/**"
      - "client/**"
      - ".github/workflows/docker.yml"

jobs:
  build-images:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker Images
        env:
          DEV_URL: "http://localhost"
          SERVER_PORT: "80"
          CLIENT_PORT: "8080"
          MONGO_PORT: "27017"
          VITE_VAPID_PUBLIC_KEY: "dummy_key"
          VAPID_PUBLIC_KEY: "dummy_key"
          VAPID_PRIVATE_KEY: "dummy_key"
        run: docker compose -f docker-compose.yml -f docker-compose.dbs.yml -f docker-compose.services.yml -f docker-compose.simulators.yml -f docker-compose.client.yml -f docker-compose.interfaces.yml build
