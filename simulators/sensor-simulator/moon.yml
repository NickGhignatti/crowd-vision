language: 'typescript'

tasks:
  build:
    command: 'npm run build'
    inputs:
      - 'src/**/*'
      - 'tsconfig*.json'
      - 'package.json'
    outputs:
      - 'dist'

  lint:
    command: 'npm run lint'
    inputs:
      - 'src/**/*'
      - 'eslint.config.mjs'
      - '/tooling/eslint-config/index.mjs'

  lint-fix:
    command: 'npm run lint:fix'
    options:
      cache: false
