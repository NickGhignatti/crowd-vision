services:
  sensor-simulator:
    cpus: "3"
    build:
      context: .
    environment:
      - TELEMETRY_INGEST_SECRET=${TELEMETRY_INGEST_SECRET}
      - TELEMETRY_INGEST_URL=http://gateway/telemetry/ingest
    ports:
      - "3001:3000"
