apiVersion: v1
kind: Service
metadata:
  name: contracts-service-db
  namespace: crowdvision
spec:
  clusterIP: None
  selector:
    app: contracts-service-db
  ports:
    - port: 27017
      targetPort: 27017
      appProtocol: tcp

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: contracts-service-db
  namespace: crowdvision
spec:
  serviceName: contracts-service-db
  replicas: 1
  selector:
    matchLabels:
      app: contracts-service-db
  template:
    metadata:
      labels:
        app: contracts-service-db
    spec:
      containers:
        - name: mongodb
          image: mongo:8
          imagePullPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
          ports:
            - containerPort: 27017
          volumeMounts:
            - name: contracts-data
              mountPath: /data/db
            - name: tmp
              mountPath: /tmp
          readinessProbe:
            exec:
              command: [mongosh, --eval, "db.adminCommand('ping')"]
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
            timeoutSeconds: 5
          livenessProbe:
            exec:
              command: [mongosh, --eval, "db.adminCommand('ping')"]
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
            timeoutSeconds: 10
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
              ephemeral-storage: "100Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
              ephemeral-storage: "500Mi"
      volumes:
        - name: tmp
          emptyDir: {}

  volumeClaimTemplates:
    - metadata:
        name: contracts-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi
