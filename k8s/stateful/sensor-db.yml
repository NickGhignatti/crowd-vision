# ── Headless Service ──────────────────────────────────────────────────────────
# Was missing in the old manifest — StatefulSet DNS requires a headless Service.
apiVersion: v1
kind: Service
metadata:
  name: sensor-db
  namespace: crowdvision
spec:
  clusterIP: None
  selector:
    app: sensor-db
  ports:
    - port: 27017
      targetPort: 27017

---
# ── StatefulSet ───────────────────────────────────────────────────────────────
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: sensor-db
  namespace: crowdvision
spec:
  serviceName: sensor-db
  replicas: 1
  selector:
    matchLabels:
      app: sensor-db
  template:
    metadata:
      labels:
        app: sensor-db
    spec:
      containers:
        - name: mongodb
          image: mongo:8
          ports:
            - containerPort: 27017
          volumeMounts:
            - name: sensor-data
              mountPath: /data/db
          readinessProbe:
            exec:
              command: [mongosh, --eval, "db.adminCommand('ping')"]
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
            timeoutSeconds: 5
          livenessProbe:
            exec:
              command: [mongosh, --eval, "db.adminCommand('ping')"]
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
            timeoutSeconds: 10
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

  volumeClaimTemplates:
    - metadata:
        name: sensor-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi
        # Local k3d:           omit storageClassName — uses default "local-path"
        # Hetzner VPS:         storageClassName: hcloud-volumes
        # DigitalOcean VPS:    storageClassName: do-block-storage
