services:
  socket-server:
    cpus: "2"
    build:
      context: .
    container_name: crowd-vision-socket
    environment:
      - PORT=3000
      - REDIS_URL=redis://redis:6379
      - FRONTEND_URL=${DEV_URL}:${FRONTEND_PORT}
    depends_on:
      - redis
