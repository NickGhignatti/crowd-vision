# Backend integration-test harness — its pytest suite runs against a composed
# stack via `just test integration`, not a moon `:test` task; `language: python` still gives it `deps` and registers it with moon.
language: 'python'
