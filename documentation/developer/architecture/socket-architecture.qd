.docname {Socket Service Architecture}
.include {docs}

**Bounded context:** real-time transport (generic) · **Stack:** Rust / Axum / socketioxide · **Code-level walkthrough:** [Socket Service](../services/socket.qd)

The socket-service is the bridge between the broker and the browser. It carries per-building telemetry and alerts to connected clients over WebSockets. It is classified as a **generic subdomain** (see [Telemetry Distribution](../domain/telemetry-distribution.qd)): pure transport, with no domain model and no database.

---

## Architectural Style

Deliberately the simplest service in the system: **no persistence and no domain model**. Flat modules — no ports-and-adapters layering, because there is no domain logic to isolate. Everything splits cleanly into two kinds of code.

- **The functional core** is pure and carries every unit test: `rooms.rs` (`room_for_building`, `room_for_domain`, `building_id_from_channel`), `auth.rs` (`authenticate_claims_header`: claims header → `Identity`), and `relay.rs` (`telemetry_delivery`, `notification_delivery`: a Redis message → the room it belongs in). String and JSON manipulation only, no I/O, no socket types.
- **The imperative shell** is everything that touches the outside world: `handlers.rs` runs the connect middleware, joins and leaves rooms, and emits; `server.rs` builds the Axum router, the CORS layer, and the Redis subscriber loop; `main.rs` binds the port and installs the shutdown signal.

The connect middleware decodes the mesh-injected `x-gateway-claims` header on the WebSocket handshake (Istio's `RequestAuthentication` already verified it once, upstream — the same trust model every other migrated service uses) and stores the resulting `Identity` in the socket's extensions, server-side.

.mermaid
    graph LR
        subgraph "Imperative Shell"
            SRV["server.rs\ncomposition root + Redis loop"]
            HND["handlers.rs\nconnect · subscribe · emit"]
        end
        subgraph "Functional Core"
            AUTH["auth.rs\nauthenticate_claims_header"]
            REL["relay.rs\ntelemetry_delivery · notification_delivery"]
            ROOMS["rooms.rs\nroom_for_building · room_for_domain\nbuilding_id_from_channel"]
        end
        BR1[(broker: notifications)] --> SRV
        BR2[(broker: telemetry:filtered:*)] --> SRV
        SRV --> REL
        SRV -->|"io.ns('/', on_connect.with(authenticate))"| HND
        HND --> AUTH
        HND --> ROOMS
        REL --> ROOMS
        SRV -->|room-scoped| CL[Connected clients]
        SRV -->|room-scoped / broadcast| CL

### Components & Connectors diagram

![Socket Service C&C diagram](images/cc-socket-service.svg)

---

## Key Architectural Decisions

- **Pure transport, no model.** Treating real-time delivery as a generic subdomain keeps all domain logic out of it and concentrates modelling effort on the core services.
- **Identity trusted from the mesh, not re-verified.** The handshake decodes an already-verified claims header instead of checking a JWT signature itself; the resulting identity is server-held for the life of the connection, so a client can never join a domain or building room it isn't entitled to.
- **Room-scoped telemetry, domain-scoped notifications.** Clients receive readings only for buildings they have joined, and notifications only for their own domain; tenants stay isolated. Only a notification with no domain (a system-wide message) is broadcast.
- **Stateless relay.** The service holds no durable state, so an instance can be replaced freely.
- **Scaling is an explicit concern.** Running more than one instance requires sticky sessions for the handshake and a broker-backed adapter to deliver across instances — a known extension point rather than a current capability.

---

## Integration

- Subscribes to the alert channel and the per-building telemetry feeds (`telemetry:filtered:<id>`), and emits them to clients — see [Communication & Data Flow](data-flow.qd).
- Holds no database and calls no other service synchronously.

For the channel-to-event mapping, the client subscription protocol, and scaling notes, see the [Socket Service](../services/socket.qd) internals page.
