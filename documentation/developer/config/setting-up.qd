.docname {Setting Up the Environment}
.include {docs}

Welcome to the CrowdVision developer setup guide. Because CrowdVision is a multi-service architecture with real-time sockets, push notifications, and AI capabilities, getting the local environment running requires setting up several databases, environment variables, and cryptographic secrets.

Don't worry — we have provided scripts to automate the hardest parts.

---

## 🛠️ 1. Prerequisites

Before cloning the repository, ensure your local machine has the following tools installed:

*   **Node.js v24**, **Python 3.12**, **Rust (stable)** — install manually or let **mise** manage them (see below).
*   **Docker & Docker Compose** (for running the isolated databases and services)
*   **uv** (Python package manager for `agent-service` — install at <https://docs.astral.sh/uv/>)
*   **Git**
*   **`just`** (cross-platform task runner — install at <https://just.systems/>)
*   **`mise`** *(recommended)* — multi-language toolchain manager. The repo root contains a `.mise.toml` that pins Node 24, Python 3.12, Rust stable, moon, and uv. Run `mise install` once after cloning and all tools are at the correct versions on any OS. Install at <https://mise.jdx.dev/>.
*   **A local Kubernetes cluster + kubectl** (only if you plan to deploy via k8s)

---

## 📦 2. Installation

Clone the repository, then install toolchain versions and all language-specific dependencies:

.code lang:{bash}
    git clone https://github.com/NickGhignatti/crowd-vision.git
    cd crowd-vision
    mise install   # installs Node 24, Python 3.12, Rust stable, moon, uv (requires mise)
    just install

`just install` installs Node deps for `tooling/`, `tooling/eslint-config/` (the `@crowdvision/eslint-config` shared ESLint package), the client, every server service, and the sensor simulator; then runs `uv sync` for the agent-service Python deps and `cargo fetch` for the Rust contracts-service. There is no root `package.json` — each service manages its own `node_modules`. Shared JS dev tooling (semantic-release, commitlint) lives in `tooling/`; the shared ESLint flat config is the `@crowdvision/eslint-config` package at `tooling/eslint-config/`.

---

## 🔐 3. Environment Variables & Secrets

Microservices require secrets to communicate securely, encrypt tokens, and send push notifications. **Never commit `.env` files to version control.**

### Automatic Setup (Recommended)

Run the interactive setup script. It skips any variable already present in `.env`, so it is safe to re-run at any time:

.code lang:{bash}
    just env

You will be prompted for the following:

.var {headers}
   - Variable
   - Default
   - Description

.tablebyrows {.headers}
    - - `MONGO_PORT`
      - `27017`
      - Exposed port for MongoDB containers (local Docker only).
    - - `CLIENT_PORT`
      - `8080`
      - Port the Vue client is served on.
    - - `BACKEND_URL`
      - `http://localhost`
      - URL of the API gateway. Use `http://localhost` for Docker dev, `http://crowdvision.local` for k8s dev.
    - - `CLIENT_URL`
      - `http://localhost:8080`
      - Browser origin used for CORS. Must match where you open the app.
    - - `VITE_SIMULATOR_URL_AQ`
      - `http://localhost/simulator/aq`
      - Air quality simulator endpoint (external service).
    - - `VITE_SIMULATOR_URL_TP`
      - `http://localhost/simulator/tp`
      - Temperature/presence simulator endpoint (external service).
    - - `GOOGLE_API_KEY`
      - *(empty)*
      - Gemini API key for the AI agent. Optional — service boots without it.
    - - `DEEPSEEK_API_KEY`
      - *(empty)*
      - DeepSeek API key. Optional — service boots without it.

The following secrets are **generated automatically** by `just env` and written to `.env` — you do not need to supply them manually:

*   `JWT_SECRET` — used by Auth, Twin, and Notification services to sign/verify JWTs.
*   `INTERNAL_ADMIN_SECRET` — internal service-to-service credential.
*   `VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY` — web push notification keys.

.box type:{warning}
    **Security Note:** All microservices that share `JWT_SECRET` must have the exact same value. If they differ, cross-service authorization will fail with `401 Unauthorized`.

### Manual VAPID Key Generation

If you need to regenerate VAPID keys separately:

.code lang:{bash}
    node scripts/vapid.js

Copy the output into your `.env` file.

---

## 🐳 4. Running Locally with Docker Compose

With your `.env` set up, start the full development stack:

.code lang:{bash}
    just dev

This starts all services with hot-reloading. When you save a source file, the change is reflected inside the running container instantly — no manual restart needed.

**Skip heavy services you are not actively working on:**

.code lang:{bash}
    just dev exclude="agent"            # skip the Python AI service
    just dev exclude="agent simulator"  # also skip simulator containers

See the [Running the Application](running.qd) page for the full Docker Compose reference.

---

## 🌱 5. Bootstrapping the System (Provisioning)

With empty databases you cannot log in. Run the provisioning script to create the initial Global Admin account and default Domain:

.code lang:{bash}
    node scripts/ops/provision.js

Run this once after `just dev` reports all services as healthy. The script prompts you for the admin email, password, and domain name, then calls the live auth-service to register them.

*(If you are working on the Python AI Agent, you can also re-ingest documentation into the knowledge base at any time with `just agent-ingest`.)*

---

## ☸️ 6. Kubernetes Deployment

If you are deploying CrowdVision to a local or remote Kubernetes cluster, follow the steps below. For ongoing day-to-day k8s operations, see the [Kubernetes Configuration](kubernetes.qd) page.

### Step 1: Create the k3d Cluster (run once per machine)

.code lang:{bash}
    k3d cluster create crowdvision \
      --servers 1 \
      --agents 3 \
      --api-port 127.0.0.1:6550 \
      -p "80:80@loadbalancer" \
      -p "443:443@loadbalancer" \
      --k3s-arg "--disable=traefik@server:*"

.var {headers}
   - Flag
   - Purpose

.tablebyrows {.headers}
    - - `--servers 1`
      - One control plane node.
    - - `--agents 3`
      - Three worker nodes.
    - - `--api-port 127.0.0.1:6550`
      - Binds the K8s API server explicitly to localhost. **Required on Windows** — prevents `kubectl` connection failures when `host.docker.internal` resolves to a non-loopback IP.
    - - `-p "80:80@loadbalancer"`
      - Maps host port 80 to the k3d load balancer so `http://localhost/` reaches the nginx Ingress controller.
    - - `--k3s-arg "--disable=traefik@server:*"`
      - Disables the default Traefik ingress. CrowdVision uses nginx instead.

Verify the cluster is reachable:

.code lang:{bash}
    kubectl cluster-info
    kubectl get nodes
    # All nodes should show STATUS: Ready

### Step 2: Configure Local DNS

.code lang:{bash}
    just k8s-hosts
    # Prints: 127.0.0.1   crowdvision.local

Open your hosts file as Administrator and add that line:
*   Windows: `C:\Windows\System32\drivers\etc\hosts`
*   Linux/Mac: `/etc/hosts`

### Step 3: Add GHCR Credentials to `.env`

The cluster needs credentials to pull images from GitHub Container Registry. Add to your `.env`:

.code lang:{bash}
    GHCR_USERNAME=nickghignatti
    GHCR_TOKEN=<your GitHub PAT with read:packages scope>

Create a PAT at <https://github.com/settings/tokens> with `read:packages` scope.

### Step 4: Full Bootstrap

.code lang:{bash}
    just k8s-setup

This single command runs in order:
1. Installs the nginx Ingress Controller into `ingress-nginx` namespace.
2. Waits until the Ingress controller pod is `Running`.
3. Creates the `crowdvision` namespace.
4. Runs `just k8s-secrets` — creates all Kubernetes Secret objects from your `.env`.
5. Applies ConfigMaps, StatefulSets (databases), Deployments, Ingress, and monitoring.

.box type:{warning}
    `just k8s-secrets` must run after the namespace exists. `just k8s-setup` handles this ordering automatically. If you run `just k8s-secrets` manually before creating the namespace, it will fail with "namespaces not found".

### Accessing the Platform

After `just k8s-setup` completes, the platform is available at `http://crowdvision.local`. All services route through the nginx Ingress controller on port 80.

.var {headers}
   - Interface
   - URL

.tablebyrows {.headers}
    - - Vue Frontend
      - `http://crowdvision.local/`
    - - Auth API
      - `http://crowdvision.local/auth/`
    - - Twin API
      - `http://crowdvision.local/twin/`
    - - Sensor API
      - `http://crowdvision.local/sensor/`
    - - Socket.IO
      - `http://crowdvision.local/socket.io/`
    - - Grafana
      - `http://crowdvision.local/grafana/`
    - - Prometheus
      - `http://crowdvision.local/prometheus/`
