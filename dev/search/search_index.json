{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"CrowdVision Developer Guide","text":"<p>CrowdVision is a real-time crowd monitoring platform built on a digital twin paradigm. It lets administrators observe, in 3D, the live occupancy and environmental state of every room in a building \u2014 streamed from IoT sensors.</p> <p>The repository is a Lerna monorepo (MEVN stack) deployed entirely via Docker Compose with a Caddy reverse-proxy gateway.</p>"},{"location":"#packages","title":"Packages","text":"Package Description <code>client</code> Vue 3 + TresJS + Tailwind SPA <code>server/auth-service</code> User auth, JWT, domain management, OIDC/SSO <code>server/twin-service</code> CRUD for buildings and rooms <code>server/socket-service</code> Socket.IO gateway bridging Redis pub/sub \u2192 browser <code>server/notification-service</code> Web Push (VAPID) + Redis pub/sub dispatch <code>server/llm-service</code> (experimental) Fastify + LangChain AI service"},{"location":"#quick-start","title":"Quick start","text":"<pre><code>npm run dev     # setup + docker compose watch (hot reload + DB GUIs)\nnpm start       # production-like, detached\nnpm run stop    # tear down\nnpm test        # all service tests in parallel\n</code></pre>"},{"location":"architecture/","title":"Architecture","text":""},{"location":"architecture/#high-level-topology","title":"High-level topology","text":"<p>All HTTP/WebSocket traffic enters through a single Caddy gateway on port 80. Caddy strips the service-prefix and reverse-proxies to the target container on the internal Docker bridge network.</p> <p>Each service is isolated on two networks: <code>gateway-net</code> (shared) and a private network for its own MongoDB instance (<code>auth-net</code>, <code>twin-net</code>, <code>notification-net</code>). This enforces the Database-per-Service pattern.</p> <pre><code>graph TD\n    Browser[\"Browser / Client\"] --&gt;|HTTP + WS| Caddy[\"Caddy Gateway :80\"]\n    IoT[\"IoT Sensors\"] --&gt;|HTTP POST /notification/trigger| Caddy\n\n    subgraph Internal Docker Network\n        Caddy --&gt;|/auth/*| Auth[\"auth-service :3000\"]\n        Caddy --&gt;|/twin/*| Twin[\"twin-service :3000\"]\n        Caddy --&gt;|/notification/*| Notif[\"notification-service :3000\"]\n        Caddy --&gt;|/socket.io/*| Socket[\"socket-server :3000\"]\n\n        Notif --&gt;|PUBLISH notifications| Redis[(\"Redis broker\")]\n        Socket --&gt;|SUBSCRIBE notifications| Redis\n\n        Auth --- AuthDB[(\"auth-db MongoDB\")]\n        Twin --- TwinDB[(\"twin-db MongoDB\")]\n        Notif --- NotifDB[(\"notification-db MongoDB\")]\n    end</code></pre>"},{"location":"architecture/#routing-table","title":"Routing table","text":"Path prefix Backend <code>/auth/*</code> <code>auth-service:3000</code> <code>/twin/*</code> <code>twin-service:3000</code> <code>/notification/*</code> <code>notification-service:3000</code> <code>/socket.io/*</code> <code>socket-server:3000</code> <code>/auth-db-gui/*</code> <code>auth-db-express:8081</code> (dev only) <code>/twin-db-gui/*</code> <code>twin-db-express:8081</code> (dev only)"},{"location":"architecture/#real-time-data-flow","title":"Real-time data flow","text":"<ol> <li>A sensor (or POST to <code>/notification/trigger</code>) calls <code>publishNotification</code>.</li> <li><code>notificationService</code> serialises the payload and calls <code>redisClient.publish('notifications', ...)</code>.</li> <li><code>socket-service</code> Redis subscriber receives the message \u2192 <code>io.emit('notification', data)</code> \u2192 all connected clients.</li> <li>The Vue client's <code>socket.ts</code> receives the event and prepends to the reactive <code>socketState.notifications</code> array.</li> <li><code>NotificationBell</code> badge and <code>PushNotificationToast</code> update reactively.</li> </ol>"},{"location":"data/","title":"Data Models","text":""},{"location":"data/#building-json-format","title":"Building JSON format","text":"<p>Buildings are registered via <code>POST /twin/register</code> or by uploading a JSON file in the 3D view.</p> <pre><code>{\n  \"id\": \"building-a\",\n  \"domains\": [\"university.edu\", \"engineering-dept\"],\n  \"rooms\": [\n    {\n      \"id\": \"room-101\",\n      \"capacity\": 30,\n      \"maxTemperature\": 28,\n      \"color\": \"#10b981\",\n      \"position\": { \"x\": 0, \"y\": 0, \"z\": 0 },\n      \"dimensions\": { \"width\": 5, \"height\": 3, \"depth\": 4 }\n    }\n  ]\n}\n</code></pre>"},{"location":"data/#field-reference","title":"Field reference","text":"Field Type Required Notes <code>id</code> string \u2713 Application-level unique key \u2014 not a MongoDB ObjectId <code>domains</code> string[] \u2713 Domain names this building belongs to <code>rooms[].id</code> string \u2713 Room display name / number <code>rooms[].capacity</code> number \u2713 Maximum occupants <code>rooms[].position.y</code> number \u2713 Floor level \u2014 rooms sharing the same y are on the same floor <code>rooms[].dimensions</code> {width, height, depth} \u2713 Arbitrary units \u2014 used by Three.js for box geometry <code>rooms[].maxTemperature</code> number \u2014 Alert threshold in \u00b0C (default 28 if omitted) <code>rooms[].color</code> string \u2014 Hex colour for RightMenu header and charts"},{"location":"data/#mutable-vs-immutable-room-fields","title":"Mutable vs immutable room fields","text":"<p>Only the following fields can be changed after upload (via <code>PATCH /twin/building/:bid/room/:rid</code>):</p> <ul> <li><code>id</code> \u2014 rename the room</li> <li><code>capacity</code></li> <li><code>maxTemperature</code></li> <li><code>color</code></li> </ul> <p>Position and dimensions are immutable after registration.</p>"},{"location":"data/#user-schema","title":"User schema","text":"<pre><code>interface IUser {\n  username: string        // unique\n  email: string           // unique\n  password: string        // bcrypt hash \u2014 never returned by API\n  memberships: IDomainMembership[]\n}\n\ninterface IDomainMembership {\n  domainName: string\n  role: 'owner' | 'admin' | 'viewer'\n  externalId?: string     // IdP subject claim \u2014 set by SSO flow\n}\n</code></pre>"},{"location":"data/#domain-schema","title":"Domain schema","text":"<pre><code>interface IDomain {\n  name: string            // unique \u2014 acts as the primary key\n  subdomains: string[]\n  authStrategy: 'internal' | 'oidc'\n  ssoConfig?: {\n    issuerUrl: string\n    clientId: string\n    clientSecret: string  // select: false \u2014 never returned by default\n  }\n}\n</code></pre>"},{"location":"data/#notification-payload-redis-socketio","title":"Notification payload (Redis + Socket.IO)","text":"<pre><code>interface NotificationPayload {\n  id: string              // Date.now().toString()\n  message: string\n  type: 'info' | 'alert' | 'critical'\n  timestamp: Date\n}\n</code></pre>"},{"location":"design/","title":"Design Decisions","text":""},{"location":"design/#microservices-and-database-isolation","title":"Microservices and database isolation","text":"<p>Each service owns its MongoDB instance. This prevents schema coupling, allows independent scaling, and means a slow twin query can never block authentication.</p>"},{"location":"design/#redis-pubsub-for-real-time-events","title":"Redis pub/sub for real-time events","text":"<p>Rather than polling or direct service-to-service HTTP calls, the notification pipeline uses Redis as a message broker. This decouples producers (<code>notification-service</code>) from consumers (<code>socket-service</code>) and makes it trivial to add more consumers in the future (e.g. an LLM service reacting to crowd events).</p>"},{"location":"design/#stateless-jwt-authentication","title":"Stateless JWT authentication","text":"<p>JWTs are self-contained and verified by the <code>requireAuth</code> middleware without a database round-trip. The 3-hour expiry balances session security with user convenience. Device tokens (for IoT sensors) have a 5-year expiry.</p>"},{"location":"design/#sso-oidc-with-pkce-opaque-state","title":"SSO / OIDC with PKCE (opaque state)","text":"<p>The SSO flow uses PKCE (<code>code_challenge_method: S256</code>). The OAuth <code>state</code> parameter is a random opaque token, and the PKCE verifier plus any user context (e.g. username, domain) are stored server-side (for example in Redis) keyed by that token. The callback handler only trusts data loaded from this server-side store.</p> <p>Security note</p> <p>Never encode the PKCE verifier, username, domain, or other sensitive data directly into the <code>state</code> parameter (even as base64 JSON). Because clients can tamper with <code>state</code>, such designs are vulnerable to state-tampering attacks; always keep <code>state</code> opaque and validate it against server-side state.</p>"},{"location":"design/#domain-membership-as-embedded-subdocument","title":"Domain membership as embedded subdocument","text":"<p>User memberships are embedded in the User document as an array rather than a separate collection. This simplifies queries (a single <code>User.findOne</code> returns everything) at the cost of some update complexity for membership changes. Given the expected low cardinality of memberships per user, this is the right trade-off.</p>"},{"location":"design/#vue-composition-api-with-composables-no-global-store","title":"Vue Composition API with composables (no global store)","text":"<p>Instead of Pinia or Vuex, reactive state is co-located in <code>src/composables/</code>. <code>socketState</code> in <code>socket.ts</code> is a module-level reactive object, effectively a lightweight global store for notification state. This keeps the bundle small and avoids boilerplate.</p>"},{"location":"design/#sensor-simulation","title":"Sensor simulation","text":"<p>Sensors are simulated using the Socket.IO protocol. Each simulated client identifies its room via the <code>id</code> field in the JSON payload. The many-to-one multiplexing pattern (many sensor clients \u2192 one Socket.IO server) mirrors how a real IoT deployment would work.</p>"},{"location":"design/#domains-system","title":"Domains system","text":"<p>The Domain system adds a multi-tenant layer. Buildings are scoped to one or more domain name strings. A user's access to buildings is enforced in <code>twin-service</code>: each request must include a valid JWT, and the service verifies that the user's memberships include the requested domain with a sufficient role before returning or mutating any building data. The frontend discovers accessible buildings by fetching the user's memberships and then querying <code>/twin/buildings/:domainName</code> only for domains that the user is authorised to access. This means a single user can belong to multiple organisations and see all their buildings in one view.</p>"},{"location":"llm/","title":"LLM Service Architecture","text":"<p>The LLM Service is a new microservice introduced in v2.0.0 to decouple AI logic from the core application. It acts as an intelligent layer that processes natural language and generates insights based on system data.</p>"},{"location":"llm/#technology-stack","title":"\ud83c\udfd7\ufe0f Technology Stack","text":"<p>We chose a lightweight but robust stack to handle AI requests efficiently:</p> <ul> <li>Runtime: Node.js</li> <li>Framework: Fastify (Selected for low overhead and high performance)</li> <li>Orchestration: LangChain.js</li> <li>Models:<ul> <li>DeepSeek (Chat &amp; Reasoner): Used for complex logic and cost-effective queries.</li> <li>Google Gemini (Flash Lite): Used for high-speed, multimodal tasks.</li> </ul> </li> </ul>"},{"location":"llm/#why-a-separate-service","title":"\ud83e\udde0 Why a Separate Service?","text":""},{"location":"llm/#1-isolation-of-heavy-dependencies","title":"1. Isolation of Heavy Dependencies","text":"<p>AI libraries (like LangChain) and SDKs are heavy. By isolating them in a separate container, we keep the <code>auth-service</code> and <code>twin-service</code> lightweight and fast.</p>"},{"location":"llm/#2-independent-scaling","title":"2. Independent Scaling","text":"<p>AI inference is computationally expensive and latency-prone compared to simple CRUD operations. This architecture allows us to scale the <code>llm-service</code> independently (e.g., adding more replicas) without affecting the real-time performance of the <code>socket-service</code>.</p>"},{"location":"llm/#3-model-agnosticism","title":"3. Model Agnosticism","text":"<p>The service uses <code>LangChain</code> to abstract the underlying model providers. This allows us to switch between DeepSeek, Gemini, or OpenAI by simply changing environment variables, without rewriting business logic.</p>"},{"location":"llm/#api-integration","title":"\ud83d\udd0c API &amp; Integration","text":"<p>The service exposes a REST API via Fastify.</p> <ul> <li>Prefix: <code>/api</code></li> <li>Routes: Auto-loaded from the <code>routes/</code> directory.</li> </ul>"},{"location":"llm/#example-configuration-env","title":"Example Configuration (<code>.env</code>)","text":"<p>To run the service, ensure the following API keys are set in your environment:</p> <p>```bash DEEPSEEK_API_KEY=sk-... GOOGLE_API_KEY=AIza...</p>"},{"location":"requirements/","title":"Requirements","text":""},{"location":"requirements/#implemented","title":"Implemented","text":""},{"location":"requirements/#real-time-visualisation","title":"Real-time visualisation","text":"<p>High-frequency crowd and temperature updates streamed via WebSocket (Socket.IO) from the Express gateway to the Vue client. The 3D view and dashboard table reflect live sensor data.</p>"},{"location":"requirements/#push-server-initiated-notifications","title":"Push server-initiated notifications","text":"<p>Threshold-based alerts (overcrowding, temperature exceeds <code>maxTemperature</code>) are published to Redis by the notification service, broadcast to connected clients via Socket.IO, and simultaneously delivered as Web Push notifications to subscribed browsers.</p>"},{"location":"requirements/#digital-twin-management","title":"Digital twin management","text":"<p>Full CRUD for buildings and rooms. Buildings are described by a JSON schema defining room positions and dimensions in 3D space. Rooms can be edited (capacity, temperature threshold, colour, name) after deployment.</p>"},{"location":"requirements/#multi-domain-multi-tenant","title":"Multi-domain / multi-tenant","text":"<p>The Domain system allows a single CrowdVision instance to serve multiple organisations. Users hold role-based memberships per domain. SSO via OIDC is supported for enterprise identity providers.</p>"},{"location":"requirements/#planned","title":"Planned","text":""},{"location":"requirements/#historical-data-and-analytics","title":"Historical data and analytics","text":"<p>Aggregate crowd data by hour/day with charts comparing buildings or floors over time.</p>"},{"location":"requirements/#llm-campus-assistant","title":"LLM campus assistant","text":"<p>Natural-language queries: \"Where is the least crowded study room right now?\", \"When is the canteen busiest?\"</p>"},{"location":"requirements/#ai-generated-admin-reports","title":"AI-generated admin reports","text":"<p>Weekly summaries auto-generated by the LLM service from crowd statistics.</p>"},{"location":"requirements/#intelligent-push-notifications","title":"Intelligent push notifications","text":"<p>LLM-generated contextual messages instead of static alert templates.</p>"},{"location":"requirements/#private-domains","title":"Private domains","text":"<p>Invite-only domain access with token-based invite links.</p>"}]}